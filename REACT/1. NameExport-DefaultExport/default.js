// ===================================================================//

let df = "default export";
export default df;
// => 1 file chỉ có 1 default export
// => vì chỉ 1 có 1 nên khi import có thể đặt tên tùy ý và không cần {}
// => thường dùng để export 1 hàm, class, object, biến chính của file đó
// => khi import có thể kết hợp cả default export và named export

// ===================================================================//



